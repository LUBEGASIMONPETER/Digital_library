# Frontend example env for Vite
# Set this to your backend API root (no trailing slash). When empty, the app will use relative /api paths.
VITE_BACKEND_URL=https://digital-library-fqqr.onrender.com

# Optional: comma-separated list of allowed frontend origins for the backend (set on the backend service)
# Example (backend/.env or Render env):
# FRONTEND_URLS=https://thedigitallibrarynewapp.netlify.app,https://thedigitallibraryapp.netlify.app

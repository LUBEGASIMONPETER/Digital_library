# Backend environment variables (example)
PORT=5000
MONGODB_URI=mongodb+srv://dlibrarymanagement_db_user:oq18y2GnGfZkWU58@cluster0.3zq3kme.mongodb.net/?appName=Cluster0

# Cloudinary - preferred for remote image hosting
# You can set CLOUDINARY_URL or the individual fields below.
CLOUDINARY_URL=cloudinary://<your_api_key>:<your_api_secret>@daa0hpcnf
CLOUDINARY_CLOUD_NAME=Root
CLOUDINARY_API_KEY=265436169271351
CLOUDINARY_API_SECRET=ZBJ6M


# Seed admin (optional override)
ADMIN_EMAIL=dlibrarymanagement@gmail.com
ADMIN_PASSWORD=dlibrarym@#

# ===== EMAIL CONFIGURATION =====

# Option 1: SendGrid API (RECOMMENDED for production, especially on Render/Heroku free tier)
# Free tier: 100 emails/day - Get your API key from https://sendgrid.com
SENDGRID_API_KEY=
SMTP_FROM=your-verified-sender@example.com

# Option 2: SMTP (Gmail, etc.) - Works locally but may be blocked on free hosting
# NOTE: Render free tier blocks SMTP ports 465/587. Use SendGrid instead!
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
# SMTP_FROM defaults to SMTP_USER if not set

# Optional SMTP tuning (milliseconds)
# SMTP_CONNECTION_TIMEOUT=10000
# SMTP_GREETING_TIMEOUT=10000
# SMTP_SOCKET_TIMEOUT=10000
# SMTP_REJECT_UNAUTHORIZED=true

# Alternative naming (both SMTP_* and MAILER_* are supported)
# MAILER_HOST=smtp.gmail.com
# MAILER_PORT=587
# MAILER_USER=your-email@gmail.com
# MAILER_PASS=your-app-password

# ===== OTHER CONFIGURATION =====
PORT=5000
MONGO_URI=mongodb://localhost:27017/digital_library
JWT_SECRET=change_me
